import PropTypes from 'prop-types'
import React, { Component } from 'react'

export class makeCV extends Component {
  static propTypes = {}

  render() {
    return (
      <div>makeCV</div>
    )
  }
}

export default makeCV